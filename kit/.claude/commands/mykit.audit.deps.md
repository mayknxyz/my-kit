---
description: Run dependency audit (outdated packages, vulnerabilities, licenses).
---

Invoke the mykit-ops skill to handle the **audit** step for the **deps** domain only.

User arguments: $ARGUMENTS

Follow the mykit-ops skill's audit workflow instructions with `--only deps`. Any additional user arguments should be appended.
