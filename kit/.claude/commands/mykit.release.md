---
description: Release — aborts if test plan is incomplete (safe default).
---

Invoke the mykit-ship skill to handle the **release** step.

**Test plan mode**: `abort` — if unchecked test plan items exist, stop and suggest `/mykit.release.complete` or `/mykit.release.bypass`.

User arguments: $ARGUMENTS

Follow the mykit-ship skill's release workflow instructions.
