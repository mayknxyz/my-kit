---
description: Release — marks all unchecked test plan items as complete, then releases.
---

Invoke the mykit-ship skill to handle the **release** step.

**Test plan mode**: `complete` — if unchecked test plan items exist, mark all as checked (`- [ ]` → `- [x]`) and update the PR body before proceeding.

User arguments: $ARGUMENTS

Follow the mykit-ship skill's release workflow instructions.
