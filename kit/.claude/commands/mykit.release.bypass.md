---
description: Release — removes unchecked test plan items from the PR body, then releases.
---

Invoke the mykit-ship skill to handle the **release** step.

**Test plan mode**: `bypass` — if unchecked test plan items exist, remove those lines from the PR body before proceeding.

User arguments: $ARGUMENTS

Follow the mykit-ship skill's release workflow instructions.
