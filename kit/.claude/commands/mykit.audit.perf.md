---
description: Run performance audit (anti-patterns, bottlenecks).
---

Invoke the mykit-ops skill to handle the **audit** step for the **perf** domain only.

User arguments: $ARGUMENTS

Follow the mykit-ops skill's audit workflow instructions with `--only perf`. Any additional user arguments should be appended.
