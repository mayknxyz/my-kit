---
description: Run quality audit (shellcheck, markdownlint).
---

Invoke the mykit-ops skill to handle the **audit** step for the **quality** domain only.

User arguments: $ARGUMENTS

Follow the mykit-ops skill's audit workflow instructions with `--only quality`. Any additional user arguments should be appended.
