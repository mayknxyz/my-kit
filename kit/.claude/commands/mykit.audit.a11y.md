---
description: Run accessibility audit (WCAG compliance).
---

Invoke the mykit-ops skill to handle the **audit** step for the **a11y** domain only.

User arguments: $ARGUMENTS

Follow the mykit-ops skill's audit workflow instructions with `--only a11y`. Any additional user arguments should be appended.
