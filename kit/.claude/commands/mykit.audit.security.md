---
description: Run security audit (gitleaks secret detection).
---

Invoke the mykit-ops skill to handle the **audit** step for the **security** domain only.

User arguments: $ARGUMENTS

Follow the mykit-ops skill's audit workflow instructions with `--only security`. Any additional user arguments should be appended.
